apiVersion: 1

providers:
  - name: fdt-dashboards
    orgId: 1
    folder: FlyingDarkDevTunnel
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
